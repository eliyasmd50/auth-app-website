<?php
    //arithmetic operator
    //+ - * / ** %

    $x = 6;
    $y = 7;
    $z = null;
    // $z= $x+$y;
    // $z = $x-$y;
    // $z = $x * $y;
    // $z = $x / $y;
    // $z = $x % $y;
    // $z = $x ** $y;
    echo $z;

    //increment and decrement operator

    $counter = 1;
    // $counter += $counter;
    // $counter -= $counter;
    // echo $counter;

    //operator precedence
    //()
    // **
    // * / %
    // + -
    $total = 3 - 0.000768;
    echo $total; 
?>